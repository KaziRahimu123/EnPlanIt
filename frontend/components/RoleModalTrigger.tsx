"use client";
// RoleModalTrigger is no longer used — role selection is route-based (/role-select).
// Kept as an empty no-op to avoid breaking any lingering imports.
export default function RoleModalTrigger() {
  return null;
}
